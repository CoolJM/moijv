
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Moijv - {% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    <li><a href="{{ path('list_user')}}">Liste des utilisateurs</a></li>
                    <li><a href="{{ path('list_product')}}">Liste des produits</a></li>
                    {% if is_granted('ROLE_USER') %} {# vérifie les perms de l'utilisateurs #}
                    <li><a href="{{ path('my_products')}}">Mes produits</a></li>
                    <li><a href="{{ path('add_product')}}">Ajouter un produit</a></li>
                    {% endif %}
                </ul>
            </nav>
        </header>        
                
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
